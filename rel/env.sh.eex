#!/bin/sh

# short, fly-friendly node name -- no fqdn, no cookie shenanigans.
# fly machines get a stable internal IP we could use, but for a single-node
# sqlite app there's no clustering, so a short name is enough.
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE="${RELEASE_NAME}@127.0.0.1"
